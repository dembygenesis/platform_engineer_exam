package token

import (
	"testing"
)

func TestGetAll(t *testing.T) {
	// Make new app
	/*app := fiber.New()

	// Add as route
	app.Get("/", func(ctx *fiber.Ctx) error {
		builder, err := dic.NewBuilder()
		if err != nil {
			log.Fatalf("error trying to initialize the builder: %v", err.Error())
		}
		ctn := builder.Build()

		ctx.Locals(helpers.Dependencies, ctn)
		return nil
	}, GetAll)

	// Add request object
	req := httptest.NewRequest("GET", "/", nil)

	// Then test
	resp, err := app.Test(req, 1)
	fmt.Println("resp resp", resp)
	fmt.Println("err err", err)

	buf := make([]byte, 45)
	n, err := resp.Body.Read(buf)
	if err != nil {
		require.NoError(t, err)
	}
	fmt.Println("n n", n)*/

	// So you test status codes lol
}
